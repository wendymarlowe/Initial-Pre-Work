/*Summary: Individual characters in a string literal cannot be changed.  The only way to
change a string literal is to assign it a new string.  This is called
string immutability.*/

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World";
