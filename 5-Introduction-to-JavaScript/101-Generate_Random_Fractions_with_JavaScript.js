/*Summary: This built-in JS function returns a random floating point number
between 0 and not quite 1*/

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
