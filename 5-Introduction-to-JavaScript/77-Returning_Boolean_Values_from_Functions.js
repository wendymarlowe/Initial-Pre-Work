/*Summary: We can return boolean results from a function.  Instead of writing
and if/else statement, just return the result of the boolean comparison */

function isLess(a, b) {
  // Fix this code
  return a < b;

}

// Change these values to test
console.log(isLess(15, 10));
